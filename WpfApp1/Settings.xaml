<Window x:Class="WpfApp1.Settings"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1"
        mc:Ignorable="d"
        Title="Settings" Height="500" Width="450">
    <Window.Resources>
        <DataTemplate x:Key="MailSets">
            <Border BorderBrush="Black" BorderThickness="1">
                <StackPanel>
                    <TextBlock Text="{Binding MailAdress}"/>
                    <Button x:Name="del" Content="Удалить"/>
                </StackPanel>
            </Border>
        </DataTemplate>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="75"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel>
            <Button x:Name="SelectTxt" Content="Выбрать файл"/>
            <Button x:Name="AddMail" Content="Добавить e-mail"/>
        </StackPanel>
        <ListView x:Name="mailList" ItemTemplate="{StaticResource MailSets}" Grid.Row="1"/>
    </Grid>
</Window>
